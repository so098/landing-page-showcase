# Terraform 로컬 상태 — 절대 커밋 금지(시크릿 포함 가능)
.terraform/
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# 변수 파일(민감값 가능) — 커밋 금지
*.tfvars
*.tfvars.json

# CLI 오버라이드
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl 은 커밋한다(의존성 잠금) — 무시하지 않음
